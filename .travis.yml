language: php

dist: trusty
sudo: false

php:
  - 5.4
  - 5.5
  - 5.6
  - 7.0
  - 7.1

cache:
  directories:
  - $HOME/libsodium

before_script:
  - ./ci/travis-install-libsodium.sh
  - pecl install libsodium
  - composer install

script: phpunit --coverage-text tests
